import { SafeAreaView, Text } from "react-native";

export default function NewGroup() {
  return (
    <SafeAreaView
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        paddingVertical: 20,
      }}
    >
      <Text>New Group</Text>
    </SafeAreaView>
  );
}
